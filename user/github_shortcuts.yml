name: github_shortcuts
parent: default

matches:
  - trigger: "dailygit"
    replace: "{{mydate}} Daily Commit"
    vars:
      - name: mydate
        type: date
        params:
          format: "%m/%d/%Y"